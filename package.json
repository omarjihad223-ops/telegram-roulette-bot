{
  "name": "telegram-roulette-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Full production Telegram Bot + Mini App: free reward wheel with real backend, MongoDB, inventory, referrals, admin panel.",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build:client": "npm run build --prefix client",
    "build:server": "npm run build --prefix server",
    "build": "npm run install:all && npm run build:client && npm run build:server",
    "start": "node server/dist/index.js",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "test": "npm run test --prefix server"
  }
}
